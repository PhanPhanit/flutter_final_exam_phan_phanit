import 'package:flutter/material.dart';

class ExploreNav extends StatelessWidget {
  const ExploreNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Explore",
        style: TextStyle(
          fontSize: 28,
        ),
      ),
    );
  }
}
